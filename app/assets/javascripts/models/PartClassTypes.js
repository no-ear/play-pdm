/**
 * Part class type collection.
 * 
 * @class PartClassTypes
 */
var PartClassTypes = Backbone.Collection.extend(
/** @lends PartClassTypes# */
{
	/**
	 * The model class that the collection contains.
	 */
	model : PartClassType
});
