/**
 * Part version collection class.
 * 
 * @class PartVersions
 */
var PartVersions = Backbone.Collection.extend(
/** @lends PartVersions# */
{
	/**
	 * The model class that the collection contains.
	 */
	model : PartVersion
});
